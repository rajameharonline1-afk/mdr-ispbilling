# Edit this file to introduce tasks to be run by cron.
# 
# Each task to run has to be defined through a single line
# indicating with different fields when the task will be run
# and what command to run for the task
# 
# To define the time you can provide concrete values for
# minute (m), hour (h), day of month (dom), month (mon),
# and day of week (dow) or use '*' in these fields (for 'any').
# 
# Notice that tasks will be started based on the cron's system
# daemon's notion of time and timezones.
# 
# Output of the crontab jobs (including errors) is sent through
# email to the user the crontab file belongs to (unless redirected).
# 
# For example, you can run a backup of all your user accounts
# at 5 a.m every week with:
# 0 5 * * 1 tar -zcf /var/backups/home.tgz /home/
# 
# For more information see the manual pages of crontab(5) and cron(8)
# 
# m h  dom mon dow   command


SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin

# =========================================================
# 1. MASTER NETWORK SYNC (MikroTik + OLT Link + Signal)
# Runs every 5 minutes
# =========================================================
#*/5 * * * * /bin/bash /var/www/isp_billing/cron/run_all_syncs.sh >> /var/log/isp_sync.log 2>&1
*/10 * * * * /usr/bin/php /var/www/isp_billing/api/olt_mac_refresh_telnet.php mode=full >> /var/www/isp_billing/storage/logs/olt_refresh_all.log 2>&1

# =========================================================
# 2. BILLING & EXPIRY (Daily Tasks)
# =========================================================
# Auto expire inactive users at 7:00 AM
#0 7 * * * /usr/bin/php /var/www/isp_billing/cron/auto_expire_inactive.php >> /var/www/isp_billing/storage/logs/auto_inactive.log 2>&1

# Laravel Schedule (If using for other tasks)
#* * * * * cd /var/www/isp_billing_sm && /usr/bin/php artisan schedule:run >> /var/www/isp_billing_sm/storage/logs/schedule.log 2>&1

# =========================================================
# 3. BACKUP & MAINTENANCE
# =========================================================
# Daily Database & File Backup at 2:00 AM
#0 2 * * * bash /var/www/isp_billing/backup.sh >> /var/www/isp_billing/storage/logs/backup_run.log 2>&1

